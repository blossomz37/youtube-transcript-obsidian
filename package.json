{
  "name": "youtube-transcript-notes",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": { "esbuild": "0.25.12", "obsidian": "1.12.3" }
}
